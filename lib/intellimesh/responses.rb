# frozen_string_literal: true

require File.join(File.dirname(__FILE__), "responses", "tracked_event_response")
require File.join(File.dirname(__FILE__), "responses", "service_request_response")

module Intellimesh
  # Contains behaviour encapsulating the ability to respond to both
  # events and service requests.
  module Responses
  end
end
